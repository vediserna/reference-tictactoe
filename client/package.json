{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "enzyme": "^2.6.0",
    "react-addons-test-utils": "^15.3.2",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "lodash": "^4.17.2",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "export NODE_PATH=./src/ && react-scripts start",
    "build": "export NODE_PATH=./src/ && react-scripts build",
    "test": "export NODE_PATH=./src/ && react-scripts test --env=jsdom  --watchAll",
    "citest": "CI=true && export NODE_PATH=./src/ && react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}

{
  "name": "an-outlandish-mean-app",
  "version": "1.0.0",
  "description": "Base for a simple nodejs server",
  "private": true,
  "main": "run.js",
  "dependencies": {
    "autoprefixer-core": "^3.1.0",
    "bcrypt-nodejs": "0.0.3",
    "body-parser": "^1.8.1",
    "cookie-parser": "^1.3.2",
    "express": "^4.10.4",
    "express-session": "^1.9.2",
    "jade": "^1.7.0",
    "javascript-brunch": ">=1.7.1",
    "jshint-brunch": "^1.7.0",
    "parallelshell": "^2.0.0",
    "socket.io": "^1.5.1"
  },
  "devDependencies": {
    "jest": "^17.0.3"
  },
  "jest": {
    "verbose": true,
    "testPathDirs": [
      "server/"
    ]
  },
  "scripts": {
    "startclient":"cd ./client && npm run start",
    "buildclient":"cd ./client && npm run build",
    "clean": "[ -d ./build ] && rm -r ./build",
    "start": "nodemon run.js",
    "createbuild" : "[ -d build ] || mkdir build ",
    "build": "npm run clean && npm run createbuild && npm run buildclient && mv client/build build/static && cp -R server build/server && cp run.js build",
    "test": "nodemon ./node_modules/.bin/jest",
    "dev":"./node_modules/.bin/parallelshell 'npm run start' 'npm run startclient'"
  }
}
